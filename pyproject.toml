[project]
name = "handpy-tool"
version = "0.1.0"
description = "HandPy (mPython) board control tool"
requires-python = ">=3.9"
dependencies = [
    "mpremote>=1.0",
    "esptool>=4.0",
    "pyserial>=3.5",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
handpy-tool = "handpy_tool:main"

[tool.setuptools]
py-modules = ["handpy_tool"]
packages = ["board"]
